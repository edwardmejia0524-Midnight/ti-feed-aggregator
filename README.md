# Threat-Intelligence Feed Aggregator

## Overview
A Python utility designed to collect, normalize, and standardize indicators of compromise (IOCs) from multiple threat intelligence sources into a unified JSON feed.

## Usage
Run the aggregator script:
```bash
python3 aggregator.py
